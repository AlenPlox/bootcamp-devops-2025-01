#!/bin/bash

find / -type f -name "sleep"
#the file is a command used for pausing a command for an specific amount of time, it's located in the binaries folder this folder contains installed utilities on the system
